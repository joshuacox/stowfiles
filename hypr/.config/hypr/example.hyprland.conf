# #######################################################################################
# AUTOGENERATED HYPR CONFIG.
# PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
# OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
# #######################################################################################


# This is an example Hyprland config file.
# Refer to the wiki for more information.
# https://wiki.hyprland.org/Configuring/Configuring-Hyprland/

# Please note not all available settings / options are set here.
# For a full list, see the wiki

# You can split this configuration into multiple files
# Create your files separately and then link them to this file like this:
# source = ~/.config/hypr/myColors.conf


################
### MONITORS ###
################

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=,preferred,auto,auto


###################
### MY PROGRAMS ###
###################

# See https://wiki.hyprland.org/Configuring/Keywords/

# Set programs that you use
$terminal = footclient
$fileManager = dolphin
$browser = firefox
$messenger = /home/thoth/bin/comms
#$signal = signal-desktop
$signal = /home/thoth/bin/signal
$menu = wofi --show drun


#################
### AUTOSTART ###
#################

# Autostart necessary processes (like notifications daemons, status bars, etc.)
# Or execute your favorite apps at launch like this:

#exec-once = chromium
exec-once = waybar
# Notification Daemon
exec-once = swaync
exec-once = foot -s
# exec-once = eww open bar
exec-once = lxqt-policykit-agent
exec-once = hypridle
exec-once = hyprpaper & nm-applet
#exec-once = $terminal
#exec-once = $messenger
# exec-once = nm-applet &
# exec-once = waybar & hyprpaper & firefox
#exec-once = waybar & hyprpaper & nm-applet



#############################
### ENVIRONMENT VARIABLES ###
#############################

# See https://wiki.hyprland.org/Configuring/Environment-variables/

env = XCURSOR_SIZE,24
env = HYPRCURSOR_SIZE,24


#####################
### LOOK AND FEEL ###
#####################

# Refer to https://wiki.hyprland.org/Configuring/Variables/

# https://wiki.hyprland.org/Configuring/Variables/#general
general { 
    gaps_in = 2
    gaps_out = 2

    border_size = 1

    # https://wiki.hyprland.org/Configuring/Variables/#variable-types for info about colors
    col.active_border = rgba(f31f1fee) rgba(ff1f19ee) 45deg
    col.inactive_border = rgba(595959aa)

    # Set to true enable resizing windows by clicking and dragging on borders and gaps
    resize_on_border = true

    # Please see https://wiki.hyprland.org/Configuring/Tearing/ before you turn this on
    allow_tearing = false

    layout = dwindle
}

# https://wiki.hyprland.org/Configuring/Variables/#decoration
decoration {
    rounding = 10

    # Change transparency of focused and unfocused windows
    active_opacity = 1.0
    inactive_opacity = 1.0

    drop_shadow = true
    shadow_range = 4
    shadow_render_power = 3
    col.shadow = rgba(1a1a1aee)

    # https://wiki.hyprland.org/Configuring/Variables/#blur
    blur {
        enabled = false
        size = 3
        passes = 1
        
        vibrancy = 0.1696
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#animations
animations {
    enabled = true

    # Default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
    # animation = specialWorkspaces, 1, 6, default, slidefadevert
    # animation = workspaces,1,8,default,slidefadevert 20%
    animation = specialWorkspace,1,8,default,slidefadevert 20%
}

# See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
dwindle {
    pseudotile = true # Master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    preserve_split = true # You probably want this
}

# See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
master {
    new_is_master = true
}

# https://wiki.hyprland.org/Configuring/Variables/#misc
misc { 
    force_default_wallpaper = -1 # Set to 0 or 1 to disable the anime mascot wallpapers
    disable_hyprland_logo = false # If true disables the random hyprland logo / anime girl background. :(
}


#############
### INPUT ###
#############

# https://wiki.hyprland.org/Configuring/Variables/#input
input {
    kb_layout = us
    kb_variant =
    kb_model =
    kb_options =
    kb_rules =

    follow_mouse = 1

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.

    touchpad {
        natural_scroll = true
        disable_while_typing = true
        tap-to-click = false
    }
}

# https://wiki.hyprland.org/Configuring/Variables/#gestures
gestures {
    workspace_swipe = true
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#per-device-input-configs for more
device {
    name = epic-mouse-v1
    sensitivity = -0.5
}


####################
### KEYBINDINGSS ###
####################

# See https://wiki.hyprland.org/Configuring/Keywords/
$mainMod = SUPER # Sets "Windows" key as main modifier

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more
bind = $mainMod, return, exec, $terminal
bind = $mainMod, C, killactive,
bind = $mainMod SHIFT, E, exit,
bind = $mainMod, F, exec, $fileManager
bind = $mainMod, V, togglefloating,
bind = $mainMod, R, exec, $menu
bind = $mainMod, P, pseudo, # dwindle
bind = $mainMod, J, togglesplit, # dwindle
bind = $mainMod CTRL SHIFT, W, exec, $browser
bind = $mainMod CTRL SHIFT, M, exec, $messenger

# Toggle control center
bind = $mainMod SHIFT, N, exec, swaync-client -t -sw

bind = WIN, F1, exec, ~/.config/hypr/gamemode.sh

# Move focus with mainMod + arrow keys
bind = $mainMod, left, movefocus, l
bind = $mainMod, right, movefocus, r
bind = $mainMod, up, movefocus, u
bind = $mainMod, down, movefocus, d

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspacesilent, 1
bind = $mainMod SHIFT, 2, movetoworkspacesilent, 2
bind = $mainMod SHIFT, 3, movetoworkspacesilent, 3
bind = $mainMod SHIFT, 4, movetoworkspacesilent, 4
bind = $mainMod SHIFT, 5, movetoworkspacesilent, 5
bind = $mainMod SHIFT, 6, movetoworkspacesilent, 6
bind = $mainMod SHIFT, 7, movetoworkspacesilent, 7
bind = $mainMod SHIFT, 8, movetoworkspacesilent, 8
bind = $mainMod SHIFT, 9, movetoworkspacesilent, 9
bind = $mainMod SHIFT, 0, movetoworkspacesilent, 10


# bind = CTRL, space, togglespecialworkspace, magic
# bind = $mainMod, S, movetoworkspace, +0
# bind = $mainMod, S, togglespecialworkspace, magic
# bind = $mainMod, S, movetoworkspace, special:magic
# bind = $mainMod, S, togglespecialworkspace, magic

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e+1
bind = $mainMod, mouse_up, workspace, e-1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow


##############################
### WINDOWS AND WORKSPACES ###
##############################

# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# See https://wiki.hyprland.org/Configuring/Workspace-Rules/ for workspace rules

# Example windowrule v1
# windowrule = float, ^(kitty)$

# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$

windowrulev2 = suppressevent maximize, class:.* # You'll probably like this.

# windowrulev2 = tag +term, class:(footclient)    # add dynamic tag `term*` to window footclient
# windowrulev2 = tag term, class:(footclient)     # toggle dynamic tag `term*` for window footclient
# windowrulev2 = tag +code, tag:cpp               # add dynamic tag `code*` to window with tag `cpp`
#
# windowrulev2 = opacity 0.8, tag:code            # set opacity for window with tag `code` or `code*`
# windowrulev2 = opacity 0.7, tag:cpp             # window with tag `cpp` will match both `code` and `cpp`, the last one will override prior match
# windowrulev2 = opacity 0.6, tag:term*           # set opacity for window with tag `term*` only, `term` will not be matched
#
# windowrulev2 = tag -code, tag:term              # remove dynamic tag `code*` for window with tag `term` or `term*`

# bind = $mod Ctrl, 2, tagwindow, alpha_0.2
# bind = $mod Ctrl, 4, tagwindow, alpha_0.4
#
# windowrulev2 = opacity 0.2 override, tag:alpha_0.2
# windowrulev2 = opacity 0.4 override, tag:alpha_0.4

# windowrule = move 100 100,^(kitty)$ # moves kitty to 100 100
# windowrule = animation popin,^(kitty)$ # sets the animation style for kitty
# windowrule = noblur,^(firefox)$ # disables blur for firefox
# windowrule = move cursor -50% -50%,^(kitty)$ # moves kitty to the center of the cursor
# windowrulev2 = bordercolor rgb(FF0000) rgb(880808),fullscreen:1 # set bordercolor to red if window is fullscreen
# windowrulev2 = bordercolor rgb(FFFF00),title:^(.*Hyprland.*)$ # set bordercolor to yellow when title contains Hyprland
# windowrule = opacity 1.0 override 0.5 override 0.8 override,^(kitty)$ # set opacity to 1.0 active, 0.5 inactive and 0.8 fullscreen for kitty
# windowrule = rounding 10,^(kitty)$ # set rounding to 10 for kitty
# windowrulev2 = stayfocused, class:^(pinentry-) # fix pinentry losing focus
#
windowrule = move 100 100,^(foot)$ # moves foot to 100 100
windowrule = animation popin,^(foot)$ # sets the animation style for foot
windowrule = rounding 1,^(foot)$ # set rounding to 10 for foot
windowrule = opacity 1.0 override 0.5 override 0.8 override,^(foot)$ # set opacity to 1.0 active, 0.5 inactive and 0.8 fullscreen for foot
workspace = 1,bordersize:1,gaps_in:1,gaps_out:1
workspace = 2,bordersize:2,gaps_in:2,gaps_out:2
workspace = 3, rounding:false, decorate:false
workspace = 4,bordersize:4,gaps_in:4,gaps_out:4
workspace = 5,bordersize:5,gaps_in:5,gaps_out:5
workspace = 6,bordersize:6,gaps_in:6,gaps_out:6
workspace = 7,bordersize:7,gaps_in:7,gaps_out:7
workspace = 8,bordersize:1,gaps_in:1,gaps_out:1
workspace = 9,bordersize:1,gaps_in:1,gaps_out:1
exec-once=foot -s 
exec-once=[workspace 0 silent] sleep 1 && footclient
#exec-once=[workspace 2 silent] $messenger
#exec-once=[workspace 3 silent] 
#exec-once=[workspace 4 silent] kitty
#exec-once=[workspace 5 silent] alacritty
#exec-once=[workspace 6 silent] footclient
#exec-once=[workspace 8 silent] signal
#exec-once=[workspace 9 silent] firefox
exec-once=[workspace 9 silent] qpwgraph
exec-once=[workspace 10 silent] chromium-browser

# footspace
exec-once=[workspace footspace silent] sleep 2 && footclient
bind = CTRL, space, togglespecialworkspace, footspace
bind = CTRL SHIFT, space, movetoworkspace, special:footspace
workspace = special:footspace, on-created-empty:footclient
# workspace = special:footspace, on-created-empty:$terminal
# workspace = special:footspace, on-created-empty:$terminal

# kitty
#workspace = special:kitty, on-created-empty:kitty
#bind = $mainMod SHIFT, K, movetoworkspace, special:kitty
#bind = $mainMod, K, togglespecialworkspace, kitty

# web browser
exec-once=[workspace webbrowser silent] $browser
workspace = special:webbrowser, on-created-empty:$browser
bind = $mainMod SHIFT, W, movetoworkspace, special:webbrowser
bind = $mainMod, W, togglespecialworkspace, webbrowser
#signal
exec-once=[workspace signal silent] signal
workspace = special:signal, on-created-empty:$signal
bind = $mainMod SHIFT, S, movetoworkspace, special:signal
bind = $mainMod, S, togglespecialworkspace, signal
# matrix
exec-once=[workspace matrix silent] /home/thoth/bin/matrix
workspace = special:matrix, on-created-empty:/home/thoth/bin/matrix
bind = $mainMod SHIFT, M, movetoworkspace, special:matrix
bind = $mainMod, M, togglespecialworkspace, matrix
# slack
exec-once=[workspace slack silent] /home/thoth/bin/myslack
workspace = special:slack, on-created-empty:/home/thoth/bin/myslack
bind = $mainMod SHIFT, K, movetoworkspace, special:slack
bind = $mainMod, K, togglespecialworkspace, slack
# discord
exec-once=[workspace discord silent] /home/thoth/bin/mydiscord
workspace = special:discord, on-created-empty:/home/thoth/bin/mydiscord
bind = $mainMod SHIFT, D, movetoworkspace, special:discord
bind = $mainMod, D, togglespecialworkspace, discord
# telegram
#workspace = special:telegram, on-created-empty:/home/thoth/bin/telegram
exec-once=[workspace telegram silent] telegram-desktop
workspace = special:telegram, on-created-empty:telegram-desktop
bind = $mainMod SHIFT, S, movetoworkspace, special:telegram
bind = $mainMod, T, togglespecialworkspace, telegram

# workspace = special:magic, on-created-empty:kitty
# workspace = name:coding, rounding:false, decorate:false, gapsin:0, gapsout:0, border:false, decorate:false, monitor:DP-1
# workspace = 8,bordersize:8
# workspace = name:Hello, monitor:DP-1, default:true
# workspace = name:gaming, monitor:desc:Chimei Innolux Corporation 0x150C, default:true
# workspace = 5, on-created-empty:[float] firefox
# workspace = special:scratchpad, on-created-empty:foot

env = QT_QPA_PLATFORM,wayland
env = QT_QPA_PLATFORMTHEME,qt6ct
env = QT_WAYLAND_DISABLE_WINDOWDECORATION,1
env = QT_AUTO_SCREEN_SCALE_FACTOR,1
env = QT_STYLE_OVERRIDE,kvantum
