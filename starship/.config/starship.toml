# Inserts a blank line between shell prompts
add_newline = false

# Replace the '❯' symbol in the prompt with '➜'
#[character] # The name of the module we are configuring is 'character'
#success_symbol = '[󰰸 ➜](bold green)' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'
command_timeout = 200
#format = "[$directory$git_branch$git_status]($style)$character"
#format = "[$directory$git_branch$git_status]($style)$character"
#format = '$username $hostname $all'

[username]
show_always = true
style_user = "green bold"
format = "[$user]($style) "

#[hostname]
#disabled = false
# Optional: configure how the hostname is displayed
#ssh_only = false
#style = "green bold"
#format = "[$user]($style) "

[character]
error_symbol = "[✗](bold red)"
success_symbol = "[❯](bold cyan)"

[directory]
truncation_length = 4
truncation_symbol = "…/"
repo_root_style = "bold cyan"
repo_root_format = "[$repo_root]($repo_root_style)[$path]($style)[$read_only]($read_only_style) "

[git_branch]
format = "[$branch]($style) "
style = "italic cyan"

[git_status]
format     = '[$all_status]($style)'
style      = "cyan"
ahead      = "⇡${count} "
diverged   = "⇕⇡${ahead_count}⇣${behind_count} "
behind     = "⇣${count} "
conflicted = " "
up_to_date = " "
untracked  = "? "
modified   = " "
stashed    = ""
staged     = ""
renamed    = ""
deleted    = ""


[shell]
disabled = false
bash_indicator = ""
xonsh_indicator = "🐍"
zsh_indicator = "[󰰸](bold green)"
powershell_indicator = ""
fish_indicator = "󰈺🐟"
nu_indicator = "☢️"
elvish_indicator = '🕺🎤🎸👑🕶️➜' # The 'success_symbol' segment is being set to '➜' with the color 'bold green'
cmd_indicator = ""
tcsh_indicator = ""
